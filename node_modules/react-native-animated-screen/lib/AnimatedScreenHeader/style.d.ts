import { ViewStyle } from 'react-native';
declare type AnimatedScreen = {
    animatedWrapper: ViewStyle;
    headerBackground: ViewStyle;
};
declare type Props = {
    backgroundColor: string;
    boxed?: boolean;
    blur?: boolean;
    withShadow?: boolean;
};
declare const _default: ({ boxed, blur, withShadow, backgroundColor, }: Props) => AnimatedScreen;
export default _default;
